# 核心运行时依赖
openai>=1.0.0
pydantic>=2.0.0
typer>=0.9.0
rich>=13.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
jinja2>=3.1.0
pyyaml>=6.0
python-dotenv>=1.0.0
fastapi>=0.104.0
uvicorn>=0.24.0

# 可选：本地截图功能（Vercel 上不可用，renderer.py 会自动降级为只生成 HTML）
# 本地需要截图时手动安装：pip install playwright>=1.40.0 && playwright install chromium
# playwright>=1.40.0
